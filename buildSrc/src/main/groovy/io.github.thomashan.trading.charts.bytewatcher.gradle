import org.gradle.api.plugins.jvm.JvmTestSuite

testing {
    suites {
        bytewatcher(JvmTestSuite) {
            dependencies {
                implementation project(":bytewatcher")
                implementation project()
            }
        }
    }
}
